<UserControl x:Class="InventoryManager.Components.RecentlySold"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InventoryManager.Components" xmlns:viewmodels="clr-namespace:InventoryManager.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:RecentlySoldViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="10*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Recently Sold" Foreground="White"/>
        </StackPanel>
        <ListView Grid.Row="1" ItemsSource="{Binding SoldItems}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="Test" />
                </DataTemplate>
            </ListView.ItemTemplate>            
        </ListView>
    </Grid>
</UserControl>
